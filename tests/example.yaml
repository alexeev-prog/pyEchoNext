metadata:
    name: pyBurnBuild
    version: 0.1.0
    description: build system written in python for projects in C, C++, Python, ASM
    language: python
    use_cmake: true
    cache_file: cache.json
    features:
        - pyechonext

compiler:
    name: gcc
    base_compiler_flags:
        - "-c"
